#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_javascript-obfuscator@0.18.1@javascript-obfuscator/bin/javascript-obfuscator" "$@"
  ret=$?
else 
  node  "$basedir/../../../_javascript-obfuscator@0.18.1@javascript-obfuscator/bin/javascript-obfuscator" "$@"
  ret=$?
fi
exit $ret
