#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_browser-sync@2.26.3@browser-sync/dist/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../_browser-sync@2.26.3@browser-sync/dist/bin.js" "$@"
  ret=$?
fi
exit $ret
